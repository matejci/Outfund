# frozen_string_literal: true

source 'https://rubygems.org'

# Had to bump Ruby version, because I'm using Ubuntu 22.04.3 which doesn't come with OpenSSL 1.1.
# Since Ruby 3.0.2 is still using OpenSSL 1.1, I couldn't compile it (without workarounds)
# so I just bumped the version to save time.
ruby '3.1.0'

gem 'rspec'
